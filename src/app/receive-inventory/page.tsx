import PlaceholderPage from "@/components/layout/PlaceholderPage";

export default function ReceiveInventoryPage() {
  return <PlaceholderPage title="Receive Inventory" activeKey="receive-inventory" />;
}
